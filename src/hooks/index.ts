// Animation System
export { 
  useAnimation, 
  useFadeIn, 
  useSlideUp, 
  useScaleIn, 
  useStaggeredAnimation 
} from './useAnimation';

// Counter Animations
export { 
  useCounter, 
  useCounterWithDelay, 
  useFastCounter, 
  useSlowCounter 
} from './useCounter';

// Counter Animation Hook (alias for StatsSection compatibility)
export { useCounterAnimation } from './useCounterAnimation';

// Legacy hooks (for backward compatibility)
export { useScrollAnimation } from './useScrollAnimation';
